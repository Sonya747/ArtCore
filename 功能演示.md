AI对话页面 -> 发送消息 -> 收到回复
生成图片页面 -> 输入描述提交 -> 自动优化提示词弹窗 -> 提交生成 -> 显示结果
我的作品页面 -> 查看作品列表
任务管理页面 -> 查看任务列表
空间资产界面 -> 查看资产列表 -> 点击资产查看详情 -> 点击新建资产 -> 填写新建资产表单提交 -> 查看资产列表 
审批中心界面 -> 查看待审批/已审批/我的提交列表 -> 点击审批查看详情 -> 新建审批 -> 填写新建审批表单 -> 表单提交
成员管理 -> 查看成员列表 -> 新增成员 -> 编辑成员


用户需要输入账号和密码后登陆才能进入系统，不同身份的用户会拥有不同权限，登录后系统会根据用户身份加载对应功能模块。

登录成功后进入系统主页。这里会展示欢迎信息以及各个功能模块的快捷入口。

接下来进入 AI 对话页面。
这个模块主要用于辅助美术人员进行灵感生成与创意讨论。用户可以直接通过自然语言描述需求，例如角色设定、场景氛围或者风格方向，系统会基于大语言模型返回建议内容，并且附带图片生成的提示词。

接下来进入图像生成页面。
用户输入原始描述后，系统不会直接生成，而是会先进入提示词优化流程。系统会先提取角色、场景、风格等关键词，再结合团队资产库中的参考素材优化提示词。随后系统会调用生图api完成图像生成，并最终返回生成结果。

然后进入“我的作品”页面。
这里会统一保存当前用户历史生成的作品，方便用户查看，也便于后续提交作品审批。
（作品和生成成功的任务是一对一或多对一的关系）

接下来是任务管理页面。
这个模块主要由团队运维监测项目运行情况、错误信息以及。任务会关联对应生成内容与作品结果，从而将 AI 图像生成纳入团队项目流程，而不仅仅是个人工具。

随后进入空间资产页面。
系统中的资产由美术负责人手动上传，作为团队的共享素材，在提示词优化过程中提供参考数据来源。点击资产后可以查看详细信息，包括标签、描述以及上传时间等内容。有权限的用户可以编辑以及删除资产。
这里也支持新建资产并上传新的项目素材。

然后是审批中心模块。
这个模块主要用于团队成果审核流程。系统会区分待审批、已审批以及我的提交等状态。点击可以查看对应审批流程的详细情况
用户创建新的审批流程时，需要从作品库里面选择作品、指定有权限的审批人


最后是成员管理模块。
这个模块只有管理员权限用户可以进入。管理员可以查看成员列表、新增成员以及编辑用户权限。系统通过权限控制区分普通成员与管理员，从而保证团队资源与管理功能的安全性。

