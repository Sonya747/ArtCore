---
description:  Guidelines for writing nextjs app
globs: 
alwaysApply: true
---

# 项目背景
本项目是一个aigc图像生成与任务管理系统

- 代码简洁、准确、易读
- 使用 TypeScript 构建，提供完整的类型定义文件
- 使用next.js,app-router

# 文件目录规范
- /app下存放路由页面，由next解析为路由
- /service下放请求接口
- /utils下放通用方法
- components下放通用组件

# 编码规范
- 使用es6标准及之后的语法作为js编码规范
- 使用 rsbuild 作为底层框架
- 使用 TypeScript 和 React 书写
- 使用 tanstack router 作为路由系统，使用文件路由规则
- 优先使用 ahooks 作为 hooks 库
- 优先使用 antd6 组件库 中的组件
- 优先使用 tailwindcss4 实现样式，除非无法实现时才创建额外的 css 文件
- 当可复用已有组件时可以复用已有组件
- 后端服务api统一在 /services 目录管理并统一导出，调用时使用 API.xx.xx 进行调用
- 尽量避免引入新依赖，严控打包体积
- 目标兼容现代浏览器
- 合理使用 React.memo、useMemo 和 useCallback 优化性能，严禁滥用
- 合理拆分组件，按照业务逻辑、ui逻辑进行合理拆分，遵循单一职责原则，且不过度拆分，严格控制出现单文件超过1000行
- 文件命名采用 小写字母和中划线- 进行拼接
- 变量们使用小驼峰命名，组件名称使用大驼峰命名，常量用全大写字母和下划线命名
- 项目支持深色模式切换，在开发时需要兼容深色模式，antd组件无需额外处理，其他自定义颜色部分需要进行兼容
