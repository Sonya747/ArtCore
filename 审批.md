## 总体布局：双视图管理面板 (Review Dashboard)

建议采用 **Tabs（切换卡）** 作为顶层导航，将“我的申请”与“待我审批”物理隔离。

### 1. 顶部工具栏 (Toolbar)
* **左侧：** Tabs 切换（`我的提交` / `待我审批`）。
* **右侧：** * **状态筛选器 (Dropdown/Select)：** 全部、待审核、已通过、已驳回。
    * **搜索框：** 按任务名称或提交人搜索。

---

### 2. 列表区域 (List/Table View)
使用响应式表格或卡片流，核心字段包括：
* **缩略图：** 生成图片的预览，点击可放大。
* **任务名称/ID：** 关联的生图任务信息。
* **当前节点：** * 在“我的提交”视图中，显示“审核人：张三”。
    * 在“待我审批”视图中，显示“提交人：李四”。
* **状态标签 (Badge)：**
    * `待审核` (黄色/多色闪烁)
    * `已通过` (绿色)
    * `未通过` (红色)
* **提交时间：** 相对时间（如：2小时前）。
* **操作列：** * `查看详情` (所有状态可见)。
    * `重新提交` (仅“未通过”状态可见)。

---

### 3. 详情与审批侧边栏 (Detail & Action Panel)
当用户点击列表中的某一项时，从右侧弹出 **Sheet (侧边抽屉)** 或进入详情页。

#### **左侧/上方：图片展示区**     
* 高清大图预览。
* **Prompt 信息卡：** 显示该图片生成的提示词、种子值、模型参数，方便审核员判断“质量是否达标”。

#### **右侧/下方：审批记录与操作区**
* **审批历史 (Timeline)：**
    * 显示流程节点：`提交申请` -> `审核意见` -> `最终结果`。
    * 每一条记录包含：操作人、时间、**批注文字**。
* **批注输入框 (Textarea)：**
    * **管理员视角：** 必填项。如果不通过，必须说明具体修改意见（如：“光影太亮，人物手指崩了”）。
    * **用户视角：** 再次提交时，可以在此输入“已根据意见调整提示词，重新生成”。
* **动作按钮组 (Action Buttons)：**
    * **管理员：** `[驳回]` (红色二次确认) 、 `[通过]` (绿色)。
    * **普通用户（驳回状态）：** `[重新发起审批]` 按钮，点击后跳转回生图模块或直接弹出参数微调框。

---

## 核心交互流程设计

### A. 用户：重新提交逻辑
* 当状态为“未通过”时，详情页应高亮显示管理员的**文字批注**。
* 提供一个“**以此参数重新生成**”的快捷键，自动填充原有的 Prompt 和参数，用户修改后生成新图，再次点击提交。系统应将新老任务进行关联，形成“版本 v1/v2”的逻辑。

### B. 管理员：审批逻辑
* 在“待我审批”视图中， Badge 应该有红点提醒。
* 审批时，支持“**一键复制 Prompt**”。有时候管理员会想亲自测试用户的提示词是否可以优化，这个小功能非常贴心。
